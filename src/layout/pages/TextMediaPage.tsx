"use client";

import { TextMediaPageProps } from "@/models/page.interface";
import { FunctionComponent, JSX } from "react";

const TextMediaPage: FunctionComponent<TextMediaPageProps> = (
  props
): JSX.Element => {
  return <section></section>;
};

export default TextMediaPage;
